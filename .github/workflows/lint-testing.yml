name: Run linter

on:
        - push

jobs:
        build:
                
                runs-on: ubuntu-latest
                steps:
                        - uses: actions/checkout@2
                        - name: test code
                          run: make lint
